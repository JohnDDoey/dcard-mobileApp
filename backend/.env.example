# Copier vers `backend/.env`. Ne pas commiter le fichier rempli.

# Compte utilisé uniquement pour Hardhat déploy (hardhat.config.js → networks.*.accounts)
# Format : 0x + 64 caractères hex (ou valeur sans 0x selon tes outils).
WALLET_PRIVATE_KEY=
